KERBALCHANGELOG
{
	showChangelog = True //optional but strongly recommended (will be created if non-existant)
	modName = Margo's Resource Monitor //required
	license = MIT
	author = Linuxgurugamer
 
  	VERSION
	{
		version = 0.0.2.1
		CHANGE
		{
			change = Added support for KerbalChangeLog
			type = update
		}
	}

 	VERSION
	{
		version = 0.0.2
		CHANGE
		{
			change = Added feature
			type = update
			subchange = Added monitoring of high values in addition to low values, useful for things like ore on mining ships
			subchange = Increased window width by 30 pixels to accomodate the additional butto
		}
	}

 	VERSION
	{
		version = 0.0.1
		CHANGE
		{
			change = Minor bug fixes 
			type = update
			subchange = Added code to only allow to be active on vessels with ModuleCommand
			subchange = Eliminated the unexpected duplicate resources if common resources are allocated
			subchange = Added hiding of window when F2 is pressed
		}
	}

	VERSION
	{
		version = 0.0.0.3
		CHANGE
		{
			change = Fixed nullref when going to the main menu due to no game loaded 
			type = update
		}
	}

	VERSION
	{
		version = 0.0.0.2
		CHANGE
		{
			change = Fixed window not resizing properly when options changed 
			type = update
		}
	}
	VERSION
	{
		version = 0.0.0.1
		CHANGE
		{
			change = Initial beta release
			type = update	
		}
	}
}